 {{-- @extends('registroIncidencias.report')

 <div class="panel-body">
     @if (count($errors) > 0)
         <ul>
             @foreach ($errors->all() as $error)
                 @yield('mensajeErrores')
             @endforeach
         </ul>
     @endif
 </div> --}}
